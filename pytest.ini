[pytest]
testpaths = tests
pythonpath = .
markers =
    browser: drives a real Chromium against a real server (tests/browser). Deselect
        with -m "not browser" when you have not run `playwright install chromium`.
