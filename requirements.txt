# Pinned so a rebuild months from now produces the same image.
Flask==3.1.3
# Imported directly (app.py, database.py) for werkzeug.security, not just via Flask.
Werkzeug==3.1.8
# Production WSGI server. See Dockerfile: exactly one worker.
gunicorn==26.0.0
# python:*-slim ships no system tzdata, and zoneinfo would silently fail over to
# UTC for the Asia/Jakarta default -- every timestamp off by 7 hours, no error.
tzdata==2026.3
# Fernet, for encrypting the Telegram bot token at rest (see database.py).
cryptography==49.0.0
